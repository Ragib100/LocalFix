# ========================================
# LocalFix Backend Environment Variables
# ========================================

# PostgreSQL Database Configuration
# For local development, use your local PostgreSQL instance
# For production (Render), use the database credentials from Render dashboard
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=localfix

# JWT Secret (IMPORTANT: Use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secure_jwt_secret_at_least_32_characters_long

# Server Configuration
PORT=5000
NODE_ENV=development
SERVER_URL=http://localhost:5000

# Frontend URL (for CORS configuration)
# Development: http://localhost:5173 (Vite default)
# Production: https://your-app.vercel.app
CLIENT_URL=http://localhost:5173

# Email Service URL
# Development: http://localhost:5001 (local email server)
# Production: http://your-vm-ip:5001 (if using separate VM)
EMAIL_SERVICE_URL=http://localhost:5001

# Supabase Configuration (REQUIRED for file storage)
# Get these from: https://app.supabase.com/project/_/settings/api
# See SUPABASE_SETUP.md for detailed setup instructions
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# Supabase Storage Buckets (must be created in Supabase dashboard):
# - profiles: Profile pictures (5MB limit, public)
# - issue-images: Issue photos (10MB limit, public)  
# - proofs: Work completion proof photos (10MB limit, public)

# Mapbox Configuration (for interactive maps)
# Get token from: https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN=your_mapbox_access_token

# Email Configuration (for OTP and notifications)
# Use Gmail app password (not regular password)
# Create app password: https://myaccount.google.com/apppasswords
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password
EMAIL_PORT=5001

# ========================================
# Notes:
# ========================================
# 1. Never commit this file with real credentials
# 2. Copy this file to .env and fill in your actual values
# 3. For production, set NODE_ENV=production
# 4. JWT_SECRET should be at least 32 characters
# 5. Ensure all Supabase buckets are created before deployment
# 6. The local 'uploads/' folder is deprecated (now using Supabase)

